{
	// Plugin-files to load
	plugins = [f"BasicWeblings.dll", f"Duckling.dll", f"Designer.dll"];

	// SFC-trees to load
	instances = {
		#include "/home/rob/sources/aglogon/logon.conf"

		productserver = {
			type = "HttpService";
			modconf = {
				prefixes = ["http://*:8080/"];
			};
			http_branch = instances.Auth.Debug;
		};
	};
}
