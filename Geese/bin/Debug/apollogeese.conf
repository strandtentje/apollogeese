{
	// Plugin-files to load
	plugins = [f"BasicWeblings.dll", f"Duckling.dll", f"Designer.dll"];

	// SFC-trees to load
	instances = {
		#include "/home/rob/sources/learnitconcept/learnitconcept.conf"

		productserver = {
			type = "HttpService";
			modconf = {
				prefixes = ["http://*:8000/"];
			};
			http_branch = instances.learnitconcept;
		};
	};
}
