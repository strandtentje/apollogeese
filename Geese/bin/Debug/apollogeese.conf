{
	// Plugin-files to load
	plugins = [f"BasicWeblings.dll", f"Duckling.dll", f"Designer.dll"];

	// SFC-trees to load
	instances = {
		productserver = {
			type = "HttpService";
			modconf = {
				prefixes = ["http://*:8000/"];
			};
			http_branch = Module(file = "/home/rob/sources/learnitconcept/learnitconcept.conf", branch = "learnitconcept");
		};
	};
}
